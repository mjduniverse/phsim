const Options = require("./objects");

/**
 * A new simulation object that will replace the 
 * @param {*} options
 * @extends PhSim.Options.Simulation 
 */

var Simulation = function(options) {

    this.options = options;
    this.layers = [];

    for(var l = 0; i < options.layers.length; l++) {
        for(var o = 0; i < options.layers[l].objUniverse.length; o++) {

        }
    }

}

